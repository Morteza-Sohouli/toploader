# Extract filename (last path segment) from URI for Content-Disposition (used by internal file-serving locations)
# Must be in http context; loaded from conf.d so variable is available in server block.
map $uri $content_disposition_filename {
    ~*/([^/]+)$ $1;
    default "download";
}
